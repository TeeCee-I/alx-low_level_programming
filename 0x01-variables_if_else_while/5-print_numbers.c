#include <stdio.h>
/**
 * main - program that prints decimal numbers 
 * Return: Returns true always
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%i", n);

	putchar('\n');

	return (0);
}
