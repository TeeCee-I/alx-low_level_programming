#include <stdio.h>

/**
 * main - Identifier to Print its name
 * @argc: Arg count
 * @argv: Args
 * Return: 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
