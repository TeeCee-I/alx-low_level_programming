#include <stdio.h>
/**
 * main - Prints all Decimal single digit numbers
 * Return: Returns true always
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
